{
  "name": "personal-website",
  "version": "1.3.3",
  "homepage": "https://aburaihan-dev.github.io",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-gravatar": "^2.6.3",
    "react-scripts": "^2.1.8"
  },
  "engines": {
    "node": ">=16.15.1",
    "npm": ">=8.11.0"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "test": "react-scripts --openssl-legacy-provider test",
    "eject": "react-scripts --openssl-legacy-provider eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b mysite-0.0.2 -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
